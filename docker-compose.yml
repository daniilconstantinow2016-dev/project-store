services:
  # 1. База Данных (с правильным Healthcheck)
  postgres:
    image: postgres:15
    container_name: cyber_postgres
    environment:
      POSTGRES_USER: cyber_user
      POSTGRES_PASSWORD: cyber_password
      POSTGRES_DB: cyber_market_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      # Проверяем утилитой pg_isready (она встроена в образ)
      test: ["CMD-SHELL", "pg_isready -U cyber_user -d cyber_market_db"]
      interval: 10s       # Проверять раз в 10 секунд (реже, чем раньше)
      timeout: 5s         # Ждать ответа 5 секунд
      retries: 5          # Если 5 раз подряд ошибка — тогда помечаем как unhealthy
      start_period: 30s   # 👈 ГЛАВНОЕ: Первые 30 сек не считаем ошибки (даем проснуться)

  # 2. Админка
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: cyber_pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@admin.com
      PGADMIN_DEFAULT_PASSWORD: root
    ports:
      - "5050:80"
    depends_on:
      postgres:
        condition: service_healthy # Ждем, пока база скажет "Я здорова"

# 3. Твое приложение
  app:
    container_name: cyber_app
    build: ./backend/services/catalog
    ports:
      - "8080:8080"
    environment:
      # 👇 ВАЖНО: Имена переменных должны совпадать с тегами `env` в Go структурах!
      - CONFIG_PATH=./config/local.yaml
      - DB_HOST=postgres          # Это имя сервиса БД (строка 2 в этом файле)
      - DB_PORT=5432
      - DB_USER=cyber_user
      - DB_PASSWORD=cyber_password
      - DB_NAME=cyber_market_db
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  postgres_data:
